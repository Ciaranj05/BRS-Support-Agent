import { configureTimesheet as configureTimesheetWithBrowser } from "../../timesheetAutomation.js";

export async function configureTimesheet(action) {
  return configureTimesheetWithBrowser(action);
}
